function go(pagina) {
  if (pagina === 'internet') {
    window.location.href = 'internet.html';
  } else if (pagina === 'datos') {
    alert('Página de datos aún no creada');
  } else if (pagina === 'llamadas') {
    alert('Página de llamadas aún no creada');
  }
}